import 'package:flutter/material.dart';

class DaysToDosListItem extends StatelessWidget {
  const DaysToDosListItem({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      
    );
  }
}